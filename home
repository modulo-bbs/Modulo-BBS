# Home strip — one plugin name per line. Order is tab order (keys 1–5).
# This file belongs to the board, not to the menu plugin. Swap mainmenu and
# the new chrome still reads this list. Delete a line to drop that tab.
# Add a third-party plugin the same way. Comments start with #.
dashboard
social
files
bulletins
